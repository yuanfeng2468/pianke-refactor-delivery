# PianKe APK V3 重构执行清单

| 编号 | 要求 | 当前结论 | 执行方式 | 验证方式 |
|---|---|---|---|---|
| P1-01 | 修复 `adminOperationConfig` 的 `configVersion` 作用域错误 | 已确认存在 | 让事务返回 `{ configVersion }`，响应只使用事务结果 | 静态检查、函数契约测试 |
| P1-02 | 运营配置、安全审计和配置审计保持同一事务 | 公共函数已支持 `dbLike`，调用方式错误 | 统一通过事务句柄写入安全审计；失败审计使用独立非事务记录 | 源码审计、语法检查 |
| P1-03 | 统一正式版本 | `package.json`、`manifest.json` 为 3.0.0/300 | 统一为 3.1.1/311；报告与构建元数据同步 | JSON 检查、版本一致性脚本 |
| P1-04 | 隐私政策地址必须与可用生产页面一致 | 当前 Space 对应 URL 无内容；旧 URL可访问 | 保留已验证可访问的正式法律页面，并在报告中记录 Space 不一致为部署门禁 | URL 可访问性记录、配置审计 |
| P1-05 | APK privacy-first | App.vue 已有 JS 门禁；uniStatistics 原生行为不能靠 JS证明 | 不执行未经确认的统计开关破坏性修改；增加 APK 验收门禁 | App 真机验收清单 |
| P2-01 | 奖励回调、对账、并发和重放真实验收 | 沙箱不能连接真实 uniCloud/广告 SDK | 增加可执行验收脚本/清单，不伪称已通过 | 交付报告 |
| P2-02 | 报告状态收敛 | 历史报告存在相互矛盾陈述 | 新建 V3 最终报告，明确不执行 H5，APK 构建与真实环境状态分开 | 文档一致性扫描 |
| APK-01 | 不构建 H5 | 用户明确要求 | 仅执行 `build:app` | 构建日志 |
| APK-02 | 生成 APK | uni-app App 构建通常输出 HBuilderX 导入目录，签名 APK需 Android/HBuilderX构建机 | 先执行 App 编译；若无 APK，提供可导入交付包和明确阻断项 | dist/build/app 检查 |
