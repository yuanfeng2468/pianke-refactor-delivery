# 片刻 PianKe APK v3.1.1 修复报告

> **状态声明（APK V3 重构后）**：本文件记录 v3.1.1 早期阶段，不能作为本次最终发布判断依据。本次任务以 `V3_APK_REFACTOR_FINAL_REPORT.md` 为权威报告；按用户要求未执行 H5 构建，仅执行 App 目标构建。

## 修复范围

本版本依据 `PR_CLOSURE_AUDIT_REPORT.md` 修复上一版本发现的 P0、P1 和 P2 问题。

| 类别 | 修复内容 | 状态 |
|---|---|---|
| H5 构建 | 对齐 Vue 3.4.21 依赖，移除过期 `@vue/shared` 覆盖；增加 H5 兼容 shim，补齐 uni-app 编译器需要的 `isInSSRComponentSetup` 与 `injectHook` 导出 | 已验证 |
| 每日统计 | 广告奖励和信息流奖励统一使用 `user_daily_stats` 的 Asia/Shanghai 业务日计数；旧 `user.daily_*` 字段仅保留为兼容镜像 | 已修复 |
| 奖励幂等 | 奖励订单幂等索引改为用户 + 请求键的复合唯一索引，匹配业务查询语义 | 已修复 |
| 信息流 schema | 补齐 `end_time`、`reward_gold`、`reward_time` 等运行时生命周期字段 | 已修复 |
| 对账部署 | 增加 `reconcileRewardOrders/trigger.json`；只有平台 timer 且 `PIANKE_RECONCILE_TIMER_ENABLED=true` 时可免管理员 token 执行 | 已修复 |
| 配置边界 | 信息流关键奖励配置继续采用 fail-closed 规则，配置不可读时停止发奖 | 已保留并验证 |

## 验证结果

执行目录：`/home/ubuntu/pianke-v3.1.1-fix`。

- H5 构建：本次 APK-only 重构未执行；请勿将本历史记录当作本次验证证据。
- App 构建：通过，输出可由 HBuilderX 导入 `dist/build/app`。
- 契约测试：通过，`schemas_checked=30`，AssetDTO 白名单、隐私门禁和待恢复订单检查均通过。
- 云函数 JavaScript 语法检查：通过。
- 数据库与云函数 JSON 解析：通过。
- 空 catch 扫描：通过。
- `git diff --check`：历史阶段记录；本次最终自检以 `/home/ubuntu/v3-apk-selfcheck-1.log` 为准。

## 部署前置条件

云端必须部署新增或变更的数据库 schema/index、`reconcileRewardOrders` 云函数及其 timer 配置。若启用自动对账，必须在服务端环境变量中设置 `PIANKE_RECONCILE_TIMER_ENABLED=true`；管理员手工对账仍要求高强度 `PIANKE_ADMIN_KEY` 或 `UNICLOUD_ADMIN_KEY`。

## 仍需真实环境执行的验收

源码门禁不能替代真实云端并发验收。发布前仍需在 uniCloud 真环境执行重复广告回调、跨用户相同幂等键、每日限额边界、事务冲突重试、定时对账、数据库唯一索引和 Android 真机安装测试。H5/App 编译已通过，但 APK 签名、安装、广告 SDK 回调和商店隐私审核仍需构建机与真实账号环境验证。
