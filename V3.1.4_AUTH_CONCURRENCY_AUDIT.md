# v3.1.4 鉴权、并发与功能重合审查报告

## 审查范围

本报告针对 `release/v3.1.3-production` 基线及 v3.1.4 工作区变更，覆盖 `uniCloud-alipay/cloudfunctions/` 下的用户入口、管理员入口、平台回调入口、定时维护入口和公开只读入口，同时检查 feed 会话、奖励订单、放松账本、兑换记录和广告日志的交叉写入。

审查结论是：**v3.1.4 工作区未发现新的 P0 鉴权漏洞；本轮发现并修复的 P1/P0 级问题已纳入代码、schema、索引和契约测试门禁。** 真实生产环境仍需按发布说明完成 timer、schema/index 和广告回调配置验收。

## 鉴权覆盖矩阵

| 入口类别 | 代表函数 | 当前边界 | UID/资源隔离 | 审查结论 |
|---|---|---|---|---|
| 用户资产与订单 | `getWalletLedger`、`getGoldLogs`、`getExchangeRecords`、`queryRewardOrder` | `requireAuth` | 由 session UID 查询；需要 UID 的入口使用 `assertRequestedUid` | 通过 |
| 用户行为与奖励 | `checkIn`、`createRewardOrder`、`reportAdCompleted`、`cancelRewardOrder`、`claimFeedExposure`、`syncRelaxStats`、`exchangeCoupon` | `requireAuth` | 用户身份来自 session，客户端 UID 只能与 session UID 一致 | 通过 |
| 广告观测与频控 | `addAdReward`、`logAdEvent`、`checkInterstitialAdFrequency`、`getFeedAds` | `requireAuth` | 记录和限流键绑定服务端 UID；客户端直接发奖被拒绝 | 通过 |
| 用户反馈与资料 | `submitFeedback`、`getUserInfo`、`initUser` 的已登录分支 | `requireAuth` | 反馈、用户资料按 session UID 写入或读取 | 通过；`submitFeedback` 已修复 token-only 前置拒绝 |
| 管理配置 | `adminOperationConfig` | 强管理员密钥，使用 secret 强度检查与 timing-safe 比较 | 不接受普通用户 token | 通过 |
| 奖励对账 | `reconcileRewardOrders` | 平台 timer 或强管理员密钥二选一 | 订单扫描和奖励内核在服务端执行 | 通过；显式声明 `pianke-common` 依赖 |
| 定时清理 | `cleanupFeedSessions`、`cleanupRateLimit` | 仅 `triggerType=timer` | 非 timer 调用立即返回 `ADMIN_UNAUTHORIZED` | 通过；v3.1.4 增加代码级边界 |
| 平台广告回调 | `userAdCallback`、`uni-ad-callback` | 服务端回调 secret 验签 | 回调中的订单、UID、广告位和场景由共享内核匹配 | 通过；两个入口没有第二套发奖逻辑 |
| 公开只读 | `getAppConfig`、`getRelaxSentences` | 无用户登录要求 | 只投影公开配置或默认文案，不返回用户资产和管理密钥 | 通过 |

## 重点鉴权检查

`addAdReward` 的入口仍要求有效用户 session，但其业务语义被限制为记录未验证尝试并返回服务端回调要求，不会依据客户端传入的奖励数额直接写入钱包或放松账本。`logAdEvent` 允许登录用户记录广告观测事件，但服务端使用 session UID，并限制事件类型、场景和字段形态；它不是奖励入口。

`getWalletLedger`、`getGoldLogs` 和 `getRelaxSentences` 的访问边界已分离：前两者只返回当前 session 用户的权威钱包流水，后者仅返回公开金句投影。`checkInterstitialAdFrequency` 在事务中以服务端 UID 和幂等请求键记录频控决策，不接受客户端身份越权。

维护入口没有把管理员密钥和普通用户认证混用。`reconcileRewardOrders` 的 timer 分支受显式启用配置保护，管理员分支要求强度足够的 secret 并进行安全比较；两个清理任务直接拒绝非 timer 调用。该代码级边界不替代云平台 trigger 配置，但可以阻断误调用和普通客户端调用。[1] [2]

## 并发冲突审查

### Feed 会话清理与重新签发

v3.1.4 将 getFeedAds 的每个会话写入放入事务，并把 `issued/active` 且未过期限定为唯一可复用范围。已终结或已过期记录不会被覆盖，重新签发会生成包含请求上下文和随机因子的新的业务键。cleanupFeedSessions 以事务更新并检查实际更新结果后再写过期日志。因此，清理与重新签发竞态不会复活旧会话，也不会把已奖励会话重新发给客户端。[3] [4]

### 回调与奖励对账

`userAdCallback`、`uni-ad-callback` 和 `reconcileRewardOrders` 最终都进入 `processRewardedVideoCallback`。该内核在同一事务内检查已有 reward grant、读取订单状态、推进状态机、更新用户日统计、写入钱包账本、写入放松账本、创建奖励凭证并更新订单。奖励凭证和账本幂等键用于防止回调重试重复发奖；对账不是第二套奖励引擎。[5] [6]

### 放松同步与兑换扣款

syncRelaxStats 按不可变批次和幂等键重放，服务端不会把客户端累计值直接覆盖到权威资产。exchangeCoupon 的幂等检查、日统计、金币扣减、放松时长发放和 exchange_record 写入现在在同一事务内执行；`exchange_record.trans_id` 也有数据库唯一索引，重复请求返回重放结果而不再次扣款或增加统计。[7] [8]

## 功能重合审查

| 入口组合 | 关系 | 风险判断 |
|---|---|---|
| `userAdCallback` / `uni-ad-callback` | 正式入口与历史兼容壳，均转发到 `handleRewardCallback` | 无第二套验签或发奖逻辑；保留兼容入口是有意设计 |
| `addAdReward` / `rewardedVideoService` | 前者是客户端直发奖拒绝和安全审计入口，后者是服务端回调事务内核 | 业务职责已分离；客户端不能绕过回调内核 |
| `reconcileRewardOrders` / `processRewardedVideoCallback` | 对账任务负责扫描和重新调用共享内核，内核负责唯一状态机和资产变更 | 无重复资产逻辑；并发由事务和幂等键约束 |
| `getGoldLogs` / `getWalletLedger` | 两个兼容查询投影都读取 `wallet_ledger`，一个返回分页展示字段，一个返回原始流水字段 | 不存在旧 `gold_logs` 与新账本并行写入；需继续保持前端字段契约 |
| `syncRelaxStats` / `exchangeCoupon` | 不同业务写入共享放松账本和用户资产 | 通过不可变账本、事务和各自幂等键隔离，不共享可变客户端累计值 |

## 本轮修复清单

| 问题 | 修复位置 | 验证方式 |
|---|---|---|
| 终态 feed 会话重签发存在局部变量/旧键风险 | `getFeedAds/index.js` | feed 终态契约、语法检查、H5 构建 |
| feed 终态重放先过期判断导致幂等失败 | `claimFeedExposure/index.js` | claim 顺序契约、奖励闭环 |
| token-only 用户反馈被前置条件拒绝 | `submitFeedback/index.js` | 语法检查和入口源码审查 |
| 同一兑换事务并发可能重复日统计/资产写入 | `exchangeCoupon/index.js`、`exchange_record.*` | 事务幂等契约、唯一索引契约 |
| 奖励待复核日志写入 schema 未允许的 pending | `rewardedVideoService.js`、`ad_log.schema.json` | reward pending log schema 契约 |
| feed 加载失败日志写入未允许的 error | `getFeedAds/index.js` | feed load-failure log status 契约 |
| 清理任务缺少代码级 timer 边界 | 两个 cleanup 入口 | maintenance timer boundary 契约 |
| 对账云函数缺少公共模块显式依赖 | `reconcileRewardOrders/package.json` | 版本契约和部署审查 |

## 验证与剩余风险

本轮通过 `phase4-contract-test.js`、`reward-closure-test.js`、全部云函数 `node --check`、`git diff --check` 和 `pnpm build:h5`。契约输出确认 `3.1.4/314`、feed 终态不可变、兑换事务幂等、奖励待复核日志 schema 和维护 timer 边界均通过。

剩余风险属于部署与环境验收，不是当前静态代码门禁失败：uniCloud 生产环境必须实际部署 schema/index；定时任务必须由云平台以 `triggerType=timer` 调用；广告供应商必须使用正式回调 secret；还需要在真实事务数据库和真机上验证并发 claim/close、重复回调、网络失败和后台恢复。[9]

## 参考文件

[1]: ./uniCloud-alipay/cloudfunctions/reconcileRewardOrders/index.js "奖励对账入口"
[2]: ./uniCloud-alipay/cloudfunctions/cleanupFeedSessions/index.js "Feed 清理定时任务"
[3]: ./uniCloud-alipay/cloudfunctions/getFeedAds/index.js "Feed 会话签发"
[4]: ./uniCloud-alipay/cloudfunctions/claimFeedExposure/index.js "Feed claim/close"
[5]: ./uniCloud-alipay/cloudfunctions/common/pianke-common/rewardCallback.js "广告回调适配器"
[6]: ./uniCloud-alipay/cloudfunctions/common/pianke-common/rewardedVideoService.js "奖励事务内核"
[7]: ./uniCloud-alipay/cloudfunctions/syncRelaxStats/index.js "放松批次同步"
[8]: ./uniCloud-alipay/cloudfunctions/exchangeCoupon/index.js "兑换事务"
[9]: ./APK_BUILD_DELIVERY_GUIDE.md "APK 构建交付说明"
